import pandas as pd


def train_model(tables: dict):
    df = tables.get("full")
    return None


class TrainModer:

    @staticmethod
    def train_model(df: pd.DataFrame) -> pd.DataFrame:

        return df