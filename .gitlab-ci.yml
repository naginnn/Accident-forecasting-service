stages:
  - build
  - deploy

image: docker:20
services:
  - docker:20-dind

build:
  stage: build
  script:
    - docker-compose build
    - docker-compose push
  only:
    - develop

deploy:
  stage: deploy
  script:
    - docker-compose up -d
  only:
    - develop

